import { initNavigation, smoothScroll } from './navigation.js';

document.addEventListener('DOMContentLoaded', () => {
  initNavigation();
  smoothScroll();
  console.log('Atelier website initialized');
});
